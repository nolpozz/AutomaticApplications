# Core runtime dependencies. Install with:  pip install -r requirements.txt
# For the full feature set (real embeddings, PDF/DOCX, provider SDKs, dashboard)
# install the extras:  pip install -e ".[all]"
#
# The pipeline is fully runnable with just these (mock LLM + embeddings,
# Markdown output). Extras unlock real providers and richer output formats.

pydantic>=2.6
pydantic-settings>=2.2
SQLAlchemy>=2.0
openpyxl>=3.1
jinja2>=3.1
PyYAML>=6.0
typer>=0.12
rich>=13.7
httpx>=0.27
numpy>=1.26
tenacity>=8.2
